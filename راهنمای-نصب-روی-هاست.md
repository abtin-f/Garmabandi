# راهنمای نصب سایت گرمابندی روی هاست cPanel

این راهنما برای بالا آوردن **نسخه فعلی** سایت (با دیتابیس فایلی) است.
مهاجرت به MySQL مرحله بعدی است و جداگانه انجام می‌شود.

---

## پیش‌نیازها (که از قبل آماده است)
- هاست با پشتیبانی Node.js ✅
- دامنه garmabandi.ir که به هاست وصل شده ✅

---

## مرحله ۱ — آپلود فایل‌ها

۱. کل پوشه پروژه را به‌صورت یک فایل zip دربیاورید
   (پوشه node_modules را اگر هست حذف کنید — حجمش زیاد است و روی هاست ساخته می‌شود)
۲. وارد cPanel شوید → **File Manager**
۳. یک پوشه جدید بسازید، مثلاً `garmabandi`، در مسیر اصلی هوم
   (نه داخل public_html)
۴. فایل zip را داخل این پوشه آپلود کنید
۵. روی فایل zip راست‌کلیک → **Extract** → استخراج کنید

ساختار نهایی باید این شکلی باشد:
```
garmabandi/
├── backend/
│   ├── server.js
│   ├── package.json
│   └── ...
└── frontend/
    ├── index.html
    └── ...
```

---

## مرحله ۲ — ساخت Node.js App

۱. در cPanel → **Setup Node.js App**
۲. دکمه **Create Application**
۳. تنظیمات:
   - **Node.js version**: ۲۰ یا ۲۲
   - **Application mode**: Production
   - **Application root**: `garmabandi/backend`
   - **Application startup file**: `server.js`
۴. دکمه **Create** را بزنید

---

## مرحله ۳ — نصب پکیج‌ها

در همان صفحه Node.js App:
- دکمه **Run NPM Install** را بزنید
- چند دقیقه صبر کنید تا تمام شود

---

## مرحله ۴ — تنظیم متغیرهای محیطی

در همان صفحه، بخش **Environment Variables**، این موارد را اضافه کنید:

| نام متغیر | مقدار |
|-----------|-------|
| JWT_SECRET | یک رشته تصادفی طولانی (دست‌کم ۳۲ کاراکتر) |
| ADMIN_SECRET | یک رمز جدید و قوی (پیش‌فرض را نگذارید) |
| ANTHROPIC_API_KEY | خالی بگذارید مگر کلید داشته باشید |

PORT را دست نزنید — هاست خودش تنظیم می‌کند.

---

## مرحله ۵ — اجرا

دکمه **Start** (یا Restart) را بزنید.
اگر وضعیت سبز شد، یعنی سایت بالا آمده.

---

## مرحله ۶ — تست

آدرس garmabandi.ir را در مرورگر باز کنید.
سایت باید نمایش داده شود.

اگر خطا دیدید → در صفحه Node.js App بخش **logs** را بررسی کنید.

---

## مرحله ۷ — فعال‌سازی SSL (HTTPS)

۱. در cPanel → **SSL/TLS Status** یا **Let's Encrypt**
۲. برای دامنه garmabandi.ir گواهی صادر کنید
۳. مطمئن شوید سایت با https:// باز می‌شود

این مرحله برای درگاه پرداخت الزامی است.

---

## ساخت حساب ادمین

پس از راه‌اندازی:
۱. در سایت یک حساب معمولی بسازید (ثبت‌نام)
۲. برای تبدیل آن به ادمین، باید درخواست make-admin با
   ADMIN_SECRET که تنظیم کردید ارسال شود
۳. سپس از حساب خارج و دوباره وارد شوید

---

## مراحل بعدی (جداگانه انجام می‌شود)

- مهاجرت دیتابیس از db.json به MySQL
- اتصال درگاه پرداخت زرین‌پال

این دو پس از مطمئن شدن از کارکرد سایت روی هاست انجام می‌شوند.
